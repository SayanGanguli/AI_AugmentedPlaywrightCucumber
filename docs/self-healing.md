# Self-healing

Locator healing should use page evidence and prefer stable semantic selectors. The starter healer returns the first candidate different from the failed locator; production implementations should add confidence scoring and approval thresholds.
