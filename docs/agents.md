# Agents

The planner creates a test plan, the generator converts it into executable artifacts, and the orchestrator coordinates the workflow. Prompts are kept beside their agent implementations for versioning and review.
