# Architecture

The framework separates test intent (`features`), browser behavior (`src`), and AI-assisted workflows (`agents` and `ai`). Playwright provides browser automation, Cucumber provides executable business scenarios, and the MCP server exposes safe automation primitives to external clients.
