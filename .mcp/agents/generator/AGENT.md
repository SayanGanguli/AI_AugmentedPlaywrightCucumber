# Generator Agent

Follow the parent `AGENTS.md` contract. Generate Cucumber features from approved requirements using the repository's existing step vocabulary. Do not alter framework implementation unless the approved plan explicitly names supporting artifacts.
